import { render } from '@testing-library/react';
import React from 'react';

import { TOCItem } from './TOCItem';

test('renders', () => {
  // const { container } = render(<TOCItem id="page" />);
  // expect(container.childNodes.length).not.toEqual(0);
});
